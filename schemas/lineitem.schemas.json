{
    "%schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "InvoiceGen JobLine (Normalized)",
    "type": "object",
    "required": ["address", "unit", "dates", "description", "qty", "rate", "line_total", "paid", "source_row"],
    "properties": {
        "address": { "type": "string", "minLength": 1},
        "unit": { "type": "string", "minLength": 1},
        "dates": { "type": "string", "format": "date"},
        "description": { "type": "string", "minLength": 1, "maxlength": 2000},
        "qty": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$"},
        "rate": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$"},
        "line_total": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$"},
        "paid": { "type": "boolean", "description": "TRUE if job has already been paid and should be excluded from invoice."},
        "source_row": { "type": "integer", "minimum": 2}
    },
    "additionalProperties": false,
    "examples": [
        {
            "address": "Elderwood",
            "unit": "A",
            "dates": "2024-11-01",
            "description": "Repair leaking toilet.",
            "qty": "1",
            "rate": "100",
            "line_total": "100",
            "paid": "FALSE",
            "source_row": 2
        }
    ]
}